class UChatDuration {
  static const emojiPopupDuration = Duration(milliseconds: 200);
  static const switcherDuration = Duration(milliseconds: 150);
  static const messageReactionDuration = Duration(milliseconds: 100);
  static const contextMenuPopupDuration = Duration(milliseconds: 200);
}
