import 'package:flutter/material.dart';

class ChatRoomMessageList extends StatelessWidget {
  const ChatRoomMessageList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Stack(
      fit: StackFit.expand,
      children: [],
    );
  }
}
