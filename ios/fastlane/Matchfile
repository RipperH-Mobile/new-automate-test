# git_url("git@gitlab.zimpligital:zimpligital/fastlane-appstore-certificates.git")
git_url(ENV["MATCH_REPO"])

storage_mode("git")

type("appstore") # The default type, can be: appstore, adhoc, enterprise or development

app_identifier([
    ENV["IOS_APP_IDENTIFIER"],
    ENV["IOS_APP_ONESIGNAL_IDENTIFIER"],
    ENV["IOS_APP_SHARE_EXTENSION_IDENTIFIER"],
])

# username(ENV["APPLE_EMAIL"]) # Your Apple Developer Portal username

# For all available options run `fastlane match --help`
# Remove the # in the beginning of the line to enable the other options

# The docs are available on https://docs.fastlane.tools/actions/match
#
# keychain_name(ENV["MATCH_KEYCHAIN_NAME"])
# keychain_password(ENV["MATCH_KEYCHAIN_PASSWORD"])


api_key_path("./fastlane/appstore_connect_api_key.json")

team_id(ENV["TEAM_ID"])